#pragma once
#include <windows.h>
#include <commctrl.h>

HWND CreateTreView(HWND hWnd) {

	HWND h;
	return h;
}

HTREEITEM AddItem(HWND hWnd, LPCWSTR name, int lvl) {
	TVITEM item;
	TVINSERTSTRUCT insert;


}